# coinbase-crypto-data-pipeline
Build a data pipeline that fetches real-time cryptocurrency market data (e.g., prices, order books, trades) from a public crypto exchange (like Coinbase), processes it, stores it in a local database, and visualizes key metrics.
